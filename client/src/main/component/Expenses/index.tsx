import React from 'react';

import PageContainer from '../common/PageContainer';

const Expenses: React.FC = () => (
  <PageContainer>
    <p>{" Expenses page ".repeat(3000)}</p>
  </PageContainer>
);

export default Expenses;
