import React from 'react';
import styled from 'styled-components';

import PageContainer from '../common/PageContainer';

const Income: React.FC = () => (
  <PageContainer>
    <p>{" Income page ".repeat(3000)}</p>
  </PageContainer>
);

export default Income;
